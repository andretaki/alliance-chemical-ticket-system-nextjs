export interface RagFilters {
    source_type_in?: string[];
    customer_email_exact?: string;
    order_id_exact?: string;
    // Add any other filter types as needed
}

// Add any other RAG query related types or functions here 